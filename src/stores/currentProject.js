const initialState = () => ({
    currentProject: {},
})

const getters = {
}
    
const mutations = {
}

const actions = {
    init({ commit }, projectId) {
    },
}

export default {
    namespaced: true,
    state: initialState,
    mutations,
    getters,
    actions,
}